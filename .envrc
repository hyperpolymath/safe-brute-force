# direnv configuration for SafeBruteForce
# Automatically loads development environment when entering directory
#
# Setup:
# 1. Install direnv: https://direnv.net/
# 2. Run: direnv allow
#
# This will:
# - Load Nix flake development environment
# - Set up Erlang/OTP and Rebar3
# - Configure shell with project-specific settings

use flake

# Project-specific environment variables
export PROJECT_ROOT="$(pwd)"
export REBAR_COLOR="always"
export ERL_AFLAGS="-kernel shell_history enabled"

# Safety reminder
echo "⚠️  Remember: SafeBruteForce is for AUTHORIZED testing only"
